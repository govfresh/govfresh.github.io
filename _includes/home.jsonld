{
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "{{ site.title }}",
    "description": "{{ site.description }}",
    "url": "{{ site.url }}",
    "sameAs": [{% for pair in site.data.site.social %}"{{ pair[1] }}"{% unless forloop.last %},{% endunless %}{% endfor %}],
    "publisher": {
        "@type": "Organization",
        "name": "{{ site.title }}",
        "url": "{{ site.url }}"
    }
}
